work fow of a model from text preprocessing


Raw Text
    ↓
Lowercase
    ↓
Tokenization
    ↓
Remove Punctuation
    ↓
Remove Stopwords (optional)
    ↓
Stemming OR Lemmatization
    ↓
Feature Extraction (BoW, TF-IDF, Word Embeddings, etc.)
    ↓
Machine Learning 